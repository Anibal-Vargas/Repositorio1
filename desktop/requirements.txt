# Dependências do app desktop de relatórios de aterramento (Windows).
# A Etapa 1 (leitor) roda só com a biblioteca padrão; Pillow é opcional
# (melhora as imagens do pacote de exemplo). As demais entram nas próximas
# etapas (geração de documentos e empacotamento).

Pillow>=10.0        # imagens (fotos nos laudos, logo na tela)
openpyxl>=3.1       # planilha resumo (.xlsx) a partir do modelo  [Etapa 2]
python-docx>=1.1    # laudos (.docx) a partir dos modelos          [Etapas 3-4]

# Opcionais:
# pywin32           # PDF via MS Word/Excel no Windows (melhor fidelidade)  [Etapa 6b]
#                   (alternativa gratuita: instalar o LibreOffice)
# pyinstaller       # apenas para gerar o .exe (ver construir_exe.bat)      [Etapa 6b]
# pyinstaller>=6.0  # empacotamento em .exe                          [Etapa 6]
